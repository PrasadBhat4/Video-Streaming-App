import React, { Component } from "react";
import ReactDOM from "react-dom";

const Modal = props => {
  return ReactDOM.createPortal(
    <div className="ui dimmer modals visible acive">
      <div className="ui standard modal visible active">kdhiosdh</div>
    </div>
  );
};
export default Modal;
